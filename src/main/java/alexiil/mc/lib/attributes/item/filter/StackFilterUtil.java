package alexiil.mc.lib.attributes.item.filter;

public enum StackFilterUtil {
    ;

    /** Attempts to find the maximum stack amount from all of the stacks in the filter. */
    public static int findMaximumStackAmount(IStackFilter filter) {
        throw new AbstractMethodError("// TODO: Impelement this!");
    }
}
